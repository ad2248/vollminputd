# Reuse the production build/package/check functions, overriding only source/version.
source /tmp/build/production.PKGBUILD
pkgver=0.0.0.test
source=("vollminputd.tar.gz")
sha256sums=('SKIP')
pkgver() {
    printf '%s' '0.0.0.test'
}

vollminputd v1 容器化集成测试套件
用户想要开发的功能
为 vollminputd 项目增加 v1 容器化黑盒端到端集成测试。

原始诉求：构建一个虚拟/容器环境，基于 pytest 框架，启动 vollminputd 守护进程，创建虚拟音频源，监听剪贴板事件，通过验证程序行为是否符合预期来判断测试成功与否。

关键方案：

容器化测试架构：使用 Podman 构建容器，在容器内完成软件包构建（makepkg）、安装、启动 daemon 和运行测试
测试框架：pytest（host 端编排）+ Python 测试脚本（容器内执行）
虚拟音频：通过 PipeWire 的 pw-cli null-sink 创建虚拟麦克风，喂入测试音频
Wayland 显示：使用 weston headless 模式模拟 Wayland 环境
结果断言：通过 wl-paste 读取剪贴板内容，验证语音识别结果


整个过程中做出的关键决策
决策	内容
音频后端	从 ALSA 切换到 PipeWire，cpal 升级到 0.18 并启用 pipewire feature
镜像策略	合并构建和测试为一个 Containerfile（单镜像方案）
网络代理	构建和测试阶段均通过代理（http://100.64.0.5:7912）访问外网
构建缓存	bind-mount host 的 ~/.cargo/registry 加速编译
源码原则	不修改 src/ 目录，保持黑盒测试的纯净性
依赖原则	项目必须在 Arch Linux 的最新默认依赖上成功构建，不锁定版本
CI 策略	v1 仅在开发机上运行，不接入 CI
功能完成后的预期效果
成为项目的标准测试入口：用户修改 src/ 代码后，只需在 tests/integration/ 目录下执行一条 pytest 命令，5 分钟内即可获得测试结果，无需手动执行复杂的容器操作。

v1 第一个测试用例：发送 3 秒真实音频 → 容器内全程运行：启动 vollminputd 守护进程 → 触发语音识别 → 断言 wl-paste 输出的文本非空（文本长度 > 0），验证最小可行流程。

并发隔离能力：已验证 4 个容器可并行运行，每个容器拥有唯一的 --instance 名称、FIFO 路径和 weston socket，测试之间无冲突，所有容器在测试结束后自动销毁（--rm 生效），podman ps -a 不留残留。

完整的测试流程：

01_build_package.py — 在容器内通过 makepkg 构建 vollminputd 的 AUR 软件包（生成 .pkg.tar.zst）
02_toggle_happy_path.py — 安装软件包 → 启动 vollminputd 守护进程（含 PipeWire/Weston）→ 通过 null-sink 注入测试音频 → 读取剪贴板 → 断言结果
所有文件统一放在 tests/integration/ 目录下，不侵入项目源码
长远目标（Cat 记录为 far-term goal 2）：使该集成测试套件成为 vollminputd 项目的标准回归测试入口，任何源码修改后跑 pytest 即可快速验证功能完整性。